# Force use of public npm registry for this project (override corporate artifactory)
registry=https://registry.npmjs.org/

# Corporate network SSL bypass (for public registry access)
strict-ssl=false
ca=null

# PNPM configuration for React Native + Expo monorepo
# Use hoisted node_modules structure for native module compatibility
node-linker=hoisted
